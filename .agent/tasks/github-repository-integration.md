# GitHub Repository Integration

Connect the existing local `main` history to the private
`SlippinDylan/Alcove` GitHub repository without including in-progress working
tree changes. Verify repository visibility, authentication, remote URL, default
branch creation, and upstream tracking. Record GitHub MCP access separately from
the authenticated `gh` CLI result when their credentials differ.
