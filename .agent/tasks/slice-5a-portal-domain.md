# Slice 5A — Portal Domain

Create the non-Codable AlcoveCore portal aggregate required by persistence and
tabs. Enforce non-empty ordered tabs, unique durable IDs, valid selected tab,
finite positive frame, standardized folder URLs, and validated icon size. Keep
all file I/O and DTOs outside AlcoveCore.
