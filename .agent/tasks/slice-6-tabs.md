# Slice 6 — Tabs

Add, switch, and close ordered folder tabs in each Portal. Reuse one grid per
window, preserve per-tab runtime selection/scroll state, persist creation order
and selected tab before updating UI, validate every added folder, and require
confirmation before removing the final tab/Portal. Do not add tab reordering.
