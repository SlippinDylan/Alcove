# Slice 5C — Persistence Integration

Connect PortalStore to production lifecycle. Restore ordered portals on launch;
persist a new portal before presenting it; persist validated frame changes in
serialization order; retain persistence errors; never create a partial window
when saving fails. Keep advanced display-origin semantics for Slice 8.
